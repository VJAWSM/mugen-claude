# Mugen Claude Dependencies
# Uses the `claude` CLI command for Claude API interaction (no SDK needed)

# Process management
psutil>=5.9.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI and UI
rich>=13.0.0
prompt-toolkit>=3.0.0

# Utilities
python-dotenv>=1.0.0
